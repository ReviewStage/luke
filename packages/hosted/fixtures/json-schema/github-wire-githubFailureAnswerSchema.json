{
  "type": "object",
  "properties": {
    "error": {
      "type": "string",
      "enum": [
        "github-unavailable"
      ]
    },
    "reason": {
      "type": "string",
      "enum": [
        "not-connected",
        "access-denied",
        "not-found",
        "empty-repository",
        "rate-limited",
        "failed"
      ]
    }
  },
  "required": [
    "error",
    "reason"
  ],
  "additionalProperties": false
}
