{
  "type": "object",
  "properties": {
    "type": {
      "type": "string",
      "enum": [
        "session.spoken"
      ]
    },
    "kind": {
      "type": "string",
      "enum": [
        "briefing",
        "arrival",
        "calendar-onboarding",
        "launch",
        "voice-preview"
      ]
    }
  },
  "required": [
    "type",
    "kind"
  ],
  "additionalProperties": false
}
