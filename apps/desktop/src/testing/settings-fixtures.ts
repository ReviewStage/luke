// The builder lives in the package that declares the schema it fills in, so
// the phone and the host read the same resting state this app's tests do.
export { settingsView, settingsVisibility } from "@sidecar/settings/testing";
