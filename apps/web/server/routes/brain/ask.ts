import { brainAskRouteHandler } from "../../hosted/brain-ask-route.js";

/** A developer's question to Luke's judgment: admitted against its conversation, then handed to the conversation's eve session. */
export default brainAskRouteHandler;
