import { brainTurnCancelRouteHandler } from "../../../hosted/brain-turn-cancel-route.js";

/** Stop one turn: eve's cancel of the turn under way, or the stamp a waiting ask's turn is cancelled by when it starts. */
export default brainTurnCancelRouteHandler;
