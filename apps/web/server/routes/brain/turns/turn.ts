import { brainTurnRouteHandler } from "../../../hosted/brain-ask-route.js";

/** Where one turn stands, by the id its ask answered or the turn's own, held for a bounded settlement when asked to wait. */
export default brainTurnRouteHandler;
